#!/bin/bash -e

# If running the Rails server, prepare database and seed admin users
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  echo "Preparing database..."
  ./bin/rails db:prepare

  echo "Running seeds..."
  ./bin/rails db:seed
fi

exec "${@}"